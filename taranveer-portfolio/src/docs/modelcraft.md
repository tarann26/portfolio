# ModelCraft

ModelCraft is a GUI tool that lets users configure neural network architectures and automatically generates training code. It simplifies creating and experimenting with deep learning models without writing boilerplate.
